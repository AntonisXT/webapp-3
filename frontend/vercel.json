{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://<RENDER_URL>/api/$1"
    },
    {
      "source": "/auth/(.*)",
      "destination": "https://<RENDER_URL>/auth/$1"
    }
  ]
}